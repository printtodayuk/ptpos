rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For this simplified app, allow reads and writes to all collections
    // to prevent permission errors during development.
    // In a production app, you would secure this.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
